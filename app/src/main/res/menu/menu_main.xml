<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

    <group
        android:id="@+id/main">

        <item android:id="@+id/flashlight" android:title="@string/flashlight"
            android:orderInCategory="100" android:showAsAction="always" />
        <item android:id="@+id/open" android:title="@string/open"
            android:orderInCategory="200" android:showAsAction="always"/>
    </group>

    <group
        android:id="@+id/core">

        <item android:id="@+id/save" android:title="Save to photos" android:orderInCategory="200"
            android:showAsAction="always"/>
    </group>


</menu>
